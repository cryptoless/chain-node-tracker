describe('Dummy test', () => {
  it('works if true is truthy', () => {
    expect(true).toBeTruthy();
  });

  // it('index is instantiable', () => {
  //   expect(new ()).toBeInstanceOf(DummyClass)
  // })
});
